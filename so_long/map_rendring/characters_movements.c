#include "../src_so_long/so_long.h"

// here the charcters_movements
void    s_direction_face(char *str)
{
    t_directions *directions;
    directions = malloc(sizeof(t_directions));
    if (!directions)
        return ; // to-do
    directions->up = ""



}